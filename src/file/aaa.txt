docker run   -it  -v主机目录:/容器目录 镜像id (把这两个目录进行相关联)  (/var/lib/docker/volumes/hqostfile/_data)

docker inspect  id  查看是否串联成工

docker run   -it  -v主机目录:/容器目录:ro  镜像id   (把这两个目录进行相关联)  (只读)

docker build -f  文件地址  -t  重命名

docker build -t your_image_name -f mydocketfile .

docker run -it --name dc02 ”/bin/sh -c /bin/bas--volumes- from dc01
zzyy/centos

docker build -t  name -f  dockerfile .

version:"3.8"
services:
 tomcat:
   image:tomcat:8.0 #代表使用哪个镜像
      port :      #代表宿主机和容器端口映射  类似 docker run -p
     -8080:8080
 redis:
  image:redis
  ports:
     -6379:6379
 mysql:
  image:mysql
  ports:
     -"3306:3306"
   environment:
  NYSQL_ROOT_PASSWORD=123456
  volume:     #docker run -v
    -/roor/mysqldata1:var/lib/mysql

 volumes:     #docker run -v  #声明数据卷

